import{_ as s,e as i,j as n,o}from"./app-ka1GL8-S.js";const a={};function c(d,e){return o(),i("div",null,e[0]||(e[0]=[n(`<h2 id="闭包-可以捕获环境的匿名函数" tabindex="-1">闭包：可以捕获环境的匿名函数</h2><blockquote><p><a href="https://github.com/rust-lang/book/blob/main/src/ch13-01-closures.md" target="_blank" rel="noopener noreferrer">ch13-01-closures.md</a><br><br><br> commit a2cb72d3ad7584cc1ae3b85f715c877872f5e3ab</p></blockquote><p>Rust 的 <strong>闭包</strong>（<em>closures</em>）是可以保存在变量中或作为参数传递给其他函数的匿名函数。你可以在一个地方创建闭包，然后在不同的上下文中执行闭包运算。不同于函数，闭包允许捕获其被定义时所在作用域中的值。我们将展示这些闭包特性如何支持代码复用和行为定制。</p><h3 id="闭包会捕获其环境" tabindex="-1">闭包会捕获其环境</h3><p>我们首先了解如何通过闭包捕获定义它的环境中的值以便之后使用。考虑如下场景：我们的 T 恤公司偶尔会向邮件列表中的某位成员赠送一件限量版的独家 T 恤作为促销。邮件列表中的成员可以选择将他们的喜爱的颜色添加到个人信息中。如果被选中的成员设置了喜爱的颜色，他们将获得那个颜色的 T 恤。如果他没有设置喜爱的颜色，他们会获赠公司当前库存最多的颜色的款式。</p><p>有很多种方式来实现这一点。例如，使用有 <code>Red</code> 和 <code>Blue</code> 两个成员的 <code>ShirtColor</code> 枚举（出于简单考虑限定为两种颜色）。我们使用 <code>Inventory</code> 结构体来代表公司的库存，它有一个类型为 <code>Vec&lt;ShirtColor&gt;</code> 的 <code>shirts</code> 字段表示库存中的衬衫的颜色。<code>Inventory</code> 上定义的 <code>giveaway</code> 方法获取免费衬衫得主所喜爱的颜色（如有），并返回其获得的衬衫的颜色。初始代码如示例 13-1 所示：</p><p><span class="filename">文件名：src/main.rs</span></p><div class="language-rust,noplayground line-numbers-mode" data-highlighter="shiki" data-ext="rust,noplayground" data-title="rust,noplayground" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span>{{#rustdoc_include ../listings/ch13-functional-features/listing-13-01/src/main.rs}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><span class="caption">示例 13-1：衬衫公司赠送场景</span></p><p><code>main</code> 函数中定义的 <code>store</code> 还剩下两件蓝衬衫和一件红衬衫，可以在限量版促销活动中赠送。我们通过调用 <code>giveaway</code> 方法，为一个期望红衬衫的用户和一个没有特定偏好的用户进行赠送。</p><p>再次强调，这段代码有多种实现方式。这里为了专注于闭包，我们继续使用已经学习过的概念，除了 <code>giveaway</code> 方法体中使用了闭包。在 <code>giveaway</code> 方法中，我们将用户偏好作为 <code>Option&lt;ShirtColor&gt;</code> 类型的参数获取，并在 <code>user_preference</code> 上调用 <code>unwrap_or_else</code> 方法。<a href="https://doc.rust-lang.org/std/option/enum.Option.html#method.unwrap_or_else" target="_blank" rel="noopener noreferrer"><code>Option&lt;T&gt;</code> 上的 <code>unwrap_or_else</code> 方法</a> 由标准库定义。它接受一个无参闭包作为参数，该闭包返回一个 <code>T</code> 类型的值（与 <code>Option&lt;T&gt;</code> 的 <code>Some</code> 变体中存储的值类型相同，这里是 <code>ShirtColor</code>）。如果 <code>Option&lt;T&gt;</code> 是 <code>Some</code> 成员，则 <code>unwrap_or_else</code> 返回 <code>Some</code> 中的值。如果 <code>Option&lt;T&gt;</code> 是 <code>None</code> 成员，则 <code>unwrap_or_else</code> 调用闭包并返回闭包的返回值。</p><p>我们将闭包表达式 <code>|| self.most_stocked()</code> 作为 <code>unwrap_or_else</code> 的参数。这是一个本身不获取参数的闭包（如果闭包有参数，它们会出现在两道竖杠之间）。闭包体调用了 <code>self.most_stocked()</code>。我们在这里定义了闭包，而 <code>unwrap_or_else</code> 的实现会在之后需要其结果的时候执行闭包。</p><p>运行代码会打印出：</p><div class="language-console line-numbers-mode" data-highlighter="shiki" data-ext="console" data-title="console" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{{#include ../listings/ch13-functional-features/listing-13-01/output.txt}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>这里有一个有趣的地方是，我们传递了一个闭包，该闭包会在当前的 <code>Inventory</code> 实例上调用 <code>self.most_stocked()</code> 方法。标准库不需要了解我们定义的 <code>Inventory</code> 或 <code>ShirtColor</code> 类型，也不需要了解我们在这个场景中要使用的逻辑。闭包捕获了对 <code>self</code>（即 <code>Inventory</code> 实例）的不可变引用，并将其与我们指定的代码一起传递给 <code>unwrap_or_else</code> 方法。相比之下，函数无法以这种方式捕获其环境。</p><h3 id="闭包类型推断和注解" tabindex="-1">闭包类型推断和注解</h3><p>函数与闭包还有更多区别。闭包通常不要求像 <code>fn</code> 函数那样对参数和返回值进行类型注解。函数需要类型注解是因为这些类型是暴露给用户的显式接口的一部分。严格定义这些接口对于确保所有人对函数使用和返回值的类型达成一致理解非常重要。与此相比，闭包并不用于这样暴露在外的接口：它们储存在变量中并被使用，不用命名它们或暴露给库的用户调用。</p><p>闭包通常较短，并且只与特定的上下文相关，而不是适用于任意情境。在这些有限的上下文中，编译器可以推断参数和返回值的类型，类似于它推断大多数变量类型的方式（尽管在某些罕见的情况下，编译器也需要闭包的类型注解）。</p><p>类似于变量，如果我们希望增加代码的明确性和清晰度，可以添加类型注解，但代价是是会使代码变得比严格必要的更冗长。为示例 13-1 中定义的闭包标注类型看起来如示例 13-2 中的定义一样。这个例子中，我们定义了一个闭包并将它保存在变量中，而不是像示例 13-1 那样在传参的地方定义它。</p><p><span class="filename">文件名：src/main.rs</span></p><div class="language-rust line-numbers-mode" data-highlighter="shiki" data-ext="rust" data-title="rust" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">{{#rustdoc_include ../listings/ch13-functional-features/listing-13-02/src/main.rs:here}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><span class="caption">示例 13-2：为闭包的参数和返回值增加可选的类型注解</span></p><p>有了类型注解，闭包的语法看起来就更像函数的语法了。如下是一个对其参数加一的函数的定义与拥有相同行为闭包语法的纵向对比。这里增加了一些空格来对齐相应部分。这展示了除了使用竖线以及一些可选语法外，闭包语法与函数语法有多么地相似：</p><div class="language-rust,ignore line-numbers-mode" data-highlighter="shiki" data-ext="rust,ignore" data-title="rust,ignore" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span>fn  add_one_v1   (x: u32) -&gt; u32 { x + 1 }</span></span>
<span class="line"><span>let add_one_v2 = |x: u32| -&gt; u32 { x + 1 };</span></span>
<span class="line"><span>let add_one_v3 = |x|             { x + 1 };</span></span>
<span class="line"><span>let add_one_v4 = |x|               x + 1  ;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>第一行展示了一个函数定义，第二行展示了一个完整标注的闭包定义。第三行闭包定义中省略了类型注解，而第四行去掉了可选的大括号，因为闭包体只有一个表达式，所以大括号是可选的。这些都是有效的闭包定义，并在调用时产生相同的行为。调用闭包是 <code>add_one_v3</code> 和 <code>add_one_v4</code> 能够编译的必要条件，因为类型将从其用法中推断出来。这类似于 <code>let v = Vec::new();</code>，Rust 需要类型注解或是某种类型的值被插入到 <code>Vec</code> 中，才能推断其类型。</p><p>对于闭包定义，编译器会为每个参数和返回值推断出一个具体类型。例如，示例 13-3 展示了一个简短的闭包定义，该闭包仅仅返回作为参数接收到的值。除了作为示例用途外，这个闭包并不是很实用。注意这个定义没有增加任何类型注解。因为没有类型注解，我们可以使用任意类型来调用这个闭包，我们在这里第一次调用时使用了 <code>String</code> 类型。但是如果我们接着尝试使用整数来调用 <code>example_closure</code>，就会得到一个错误。</p><p><span class="filename">文件名：src/main.rs</span></p><div class="language-rust,ignore,does_not_compile line-numbers-mode" data-highlighter="shiki" data-ext="rust,ignore,does_not_compile" data-title="rust,ignore,does_not_compile" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span>{{#rustdoc_include ../listings/ch13-functional-features/listing-13-03/src/main.rs:here}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><span class="caption">示例 13-3：尝试调用一个被推断为两个不同类型的闭包</span></p><p>编译器给出如下错误：</p><div class="language-console line-numbers-mode" data-highlighter="shiki" data-ext="console" data-title="console" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{{#include ../listings/ch13-functional-features/listing-13-03/output.txt}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>第一次使用 <code>String</code> 值调用 <code>example_closure</code> 时，编译器推断出 <code>x</code> 的类型以及闭包的返回类型为 <code>String</code>。接着这些类型被锁定进闭包 <code>example_closure</code> 中，如果尝试对同一闭包使用不同类型则就会得到类型错误。</p><h3 id="捕获引用或者移动所有权" tabindex="-1">捕获引用或者移动所有权</h3><p>闭包可以通过三种方式捕获其环境中的值，它们直接对应到函数获取参数的三种方式：不可变借用、可变借用和获取所有权。闭包将根据函数体中对捕获值的操作来决定使用哪种方式。</p><p>在示例 13-4 中定义了一个捕获名为 <code>list</code> 的 vector 的不可变引用的闭包，因为只需不可变引用就能打印其值：</p><p><span class="filename">文件名：src/main.rs</span></p><div class="language-rust line-numbers-mode" data-highlighter="shiki" data-ext="rust" data-title="rust" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">{{#rustdoc_include ../listings/ch13-functional-features/listing-13-04/src/main.rs}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><span class="caption">示例 13-4：定义并调用一个捕获不可变引用的闭包</span></p><p>这个示例也展示了变量可以绑定一个闭包定义，并且我们可以像使用函数名一样，使用变量名和括号来调用该闭包。</p><p>因为同时可以有多个 <code>list</code> 的不可变引用，所以在闭包定义之前，闭包定义之后调用之前，闭包调用之后代码仍然可以访问 <code>list</code>。该代码可以编译、运行并输出：</p><div class="language-console line-numbers-mode" data-highlighter="shiki" data-ext="console" data-title="console" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{{#include ../listings/ch13-functional-features/listing-13-04/output.txt}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>接下来在示例 13-5 中，我们修改闭包体让它向 <code>list</code> vector 增加一个元素。闭包现在捕获一个可变引用：</p><p><span class="filename">文件名：src/main.rs</span></p><div class="language-rust line-numbers-mode" data-highlighter="shiki" data-ext="rust" data-title="rust" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">{{#rustdoc_include ../listings/ch13-functional-features/listing-13-05/src/main.rs}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><span class="caption">示例 13-5：定义并调用一个捕获可变引用的闭包</span></p><p>代码可以编译、运行并打印：</p><div class="language-console line-numbers-mode" data-highlighter="shiki" data-ext="console" data-title="console" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{{#include ../listings/ch13-functional-features/listing-13-05/output.txt}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>注意在 <code>borrows_mutably</code> 闭包的定义和调用之间不再有 <code>println!</code>，这是因为当 <code>borrows_mutably</code> 被定义时，它捕获了对 <code>list</code> 的可变引用。闭包在被调用后就不再被使用，这时可变借用结束。因为当可变借用存在时不允许有其它的借用，所以在闭包定义和调用之间不能有不可变引用来进行打印。可以尝试在这里添加 <code>println!</code> 看看你会得到什么报错信息！</p><p>即使闭包体不严格需要所有权，如果希望强制闭包获取它在环境中所使用的值的所有权，可以在参数列表前使用 <code>move</code> 关键字。</p><p>当将闭包传递到一个新的线程时，这个技巧特别有用，因为它将数据的所有权移动到新线程中。我们将在第十六章讨论并发时详细讨论线程以及为什么你可能需要使用它们。不过现在，我们先简要探索一下如何使用需要 <code>move</code> 关键字的闭包来生成一个新线程。示例 13-6 展示了如何修改示例 13-4，以便在一个新线程中而不是在主线程中打印 vector：</p><p><span class="filename">文件名：src/main.rs</span></p><div class="language-rust line-numbers-mode" data-highlighter="shiki" data-ext="rust" data-title="rust" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">{{#rustdoc_include ../listings/ch13-functional-features/listing-13-06/src/main.rs}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><span class="caption">示例 13-6：使用 <code>move</code> 来强制闭包为线程获取 <code>list</code> 的所有权</span></p><p>我们生成了一个新的线程，并给这个线程传递一个闭包作为参数来运行，闭包体打印出列表。在示例 13-4 中，闭包仅通过不可变引用捕获了 <code>list</code>，因为这是打印列表所需的最少的访问权限。这个例子中，尽管闭包体依然只需要不可变引用，我们还是在闭包定义前写上 <code>move</code> 关键字，以确保 <code>list</code> 被移动到闭包中。新线程可能在主线程剩余部分执行完前执行完，也可能在主线程执行完之后执行完。如果主线程维护了 <code>list</code> 的所有权但却在新线程之前结束并且丢弃了 <code>list</code>，则在线程中的不可变引用将失效。因此，编译器要求 <code>list</code> 被移动到在新线程中运行的闭包中，这样引用就是有效的。试着移除 <code>move</code> 关键字，或者在闭包定义后在主线程中使用 <code>list</code>，看看你会得到什么编译器报错！</p><p><a id="storing-closures-using-generic-parameters-and-the-fn-traits"></a><br><a id="limitations-of-the-cacher-implementation"></a><br><a id="moving-captured-values-out-of-the-closure-and-the-fn-traits"></a></p><h3 id="将被捕获的值移出闭包和-fn-trait" tabindex="-1">将被捕获的值移出闭包和 <code>Fn</code> trait</h3><p>一旦闭包捕获了定义它的环境中的某个值的引用或所有权（也就影响了什么会被移 <em>进</em> 闭包，如有），闭包体中的代码则决定了在稍后执行闭包时，这些引用或值将如何处理（也就影响了什么会被移 <em>出</em> 闭包，如有）。闭包体可以执行以下任一操作：将一个捕获的值移出闭包，修改捕获的值，既不移动也不修改值，或者一开始就不从环境中捕获任何值。</p><p>闭包捕获和处理环境中的值的方式会影响闭包实现哪些 trait，而 trait 是函数和结构体指定它们可以使用哪些类型闭包的方式。根据闭包体如何处理这些值，闭包会自动、渐进地实现一个、两个或全部三个 <code>Fn</code> trait。</p><ol><li><code>FnOnce</code> 适用于只能被调用一次的闭包。所有闭包至少都实现了这个 trait，因为所有闭包都能被调用。一个会将捕获的值从闭包体中移出的闭包只会实现 <code>FnOnce</code> trait，而不会实现其他 <code>Fn</code> 相关的 trait，因为它只能被调用一次。</li><li><code>FnMut</code> 适用于不会将捕获的值移出闭包体，但可能会修改捕获值的闭包。这类闭包可以被调用多次。</li><li><code>Fn</code> 适用于既不将捕获的值移出闭包体，也不修改捕获值的闭包，同时也包括不从环境中捕获任何值的闭包。这类闭包可以被多次调用而不会改变其环境，这在会多次并发调用闭包的场景中十分重要。</li></ol><p>让我们来看示例 13-1 中使用的在 <code>Option&lt;T&gt;</code> 上的 <code>unwrap_or_else</code> 方法的定义：</p><div class="language-rust,ignore line-numbers-mode" data-highlighter="shiki" data-ext="rust,ignore" data-title="rust,ignore" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span>impl&lt;T&gt; Option&lt;T&gt; {</span></span>
<span class="line"><span>    pub fn unwrap_or_else&lt;F&gt;(self, f: F) -&gt; T</span></span>
<span class="line"><span>    where</span></span>
<span class="line"><span>        F: FnOnce() -&gt; T</span></span>
<span class="line"><span>    {</span></span>
<span class="line"><span>        match self {</span></span>
<span class="line"><span>            Some(x) =&gt; x,</span></span>
<span class="line"><span>            None =&gt; f(),</span></span>
<span class="line"><span>        }</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>回忆一下，<code>T</code> 是表示 <code>Option</code> 中 <code>Some</code> 成员中的值的类型的泛型。类型 <code>T</code> 也是 <code>unwrap_or_else</code> 函数的返回值类型：举例来说，在 <code>Option&lt;String&gt;</code> 上调用 <code>unwrap_or_else</code> 会得到一个 <code>String</code>。</p><p>接着注意到 <code>unwrap_or_else</code> 函数有额外的泛型参数 <code>F</code>。<code>F</code> 是参数 <code>f</code> 的类型，<code>f</code> 是调用 <code>unwrap_or_else</code> 时提供的闭包。</p><p>泛型 <code>F</code> 的 trait bound 是 <code>FnOnce() -&gt; T</code>，这意味着 <code>F</code> 必须能够被调用一次，没有参数并返回一个 <code>T</code>。在 trait bound 中使用 <code>FnOnce</code> 表示 <code>unwrap_or_else</code> 最多只会调用 <code>f</code> 一次。在 <code>unwrap_or_else</code> 的函数体中可以看到，如果 <code>Option</code> 是 <code>Some</code>，<code>f</code> 不会被调用。如果 <code>Option</code> 是 <code>None</code>，<code>f</code> 将会被调用一次。由于所有的闭包都实现了 <code>FnOnce</code>，<code>unwrap_or_else</code> 接受所有三种类型的闭包，十分灵活。</p><blockquote><p>注意：函数也可以实现所有的三种 <code>Fn</code> traits。如果我们要做的事情不需要从环境中捕获值，则可以在需要某种实现了 <code>Fn</code> trait 的东西时使用函数而不是闭包。举个例子，可以在 <code>Option&lt;Vec&lt;T&gt;&gt;</code> 的值上调用 <code>unwrap_or_else(Vec::new)</code>，以便在值为 <code>None</code> 时获取一个新的空的 vector。</p></blockquote><p>现在让我们来看定义在 slice 上的标准库方法 <code>sort_by_key</code>，看看它与 <code>unwrap_or_else</code> 的区别，以及为什么 <code>sort_by_key</code> 使用 <code>FnMut</code> 而不是 <code>FnOnce</code> 作为 trait bound。这个闭包以一个 slice 中当前被考虑的元素的引用作为参数，并返回一个可以排序的 <code>K</code> 类型的值。当你想按照 slice 中每个元素的某个属性进行排序时，这个函数非常有用。在示例 13-7 中，我们有一个 <code>Rectangle</code> 实例的列表，并使用 <code>sort_by_key</code> 按 <code>Rectangle</code> 的 <code>width</code> 属性对它们从低到高排序：</p><p><span class="filename">文件名：src/main.rs</span></p><div class="language-rust,noplayground line-numbers-mode" data-highlighter="shiki" data-ext="rust,noplayground" data-title="rust,noplayground" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span>{{#rustdoc_include ../listings/ch13-functional-features/listing-13-07/src/main.rs}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><span class="caption">示例 13-7：使用 <code>sort_by_key</code> 对长方形按宽度排序</span></p><p>代码输出：</p><div class="language-console line-numbers-mode" data-highlighter="shiki" data-ext="console" data-title="console" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{{#include ../listings/ch13-functional-features/listing-13-07/output.txt}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>sort_by_key</code> 被定义为接收一个 <code>FnMut</code> 闭包的原因是它会多次调用这个闭包：对 slice 中的每个元素调用一次。闭包 <code>|r| r.width</code> 不捕获、修改或将任何东西移出它的环境，所以它满足 trait bound 的要求。</p><p>相比之下，示例 13-8 展示了一个只实现了 <code>FnOnce</code> trait 的闭包的例子，因为它从环境中移出了一个值。编译器不允许我们在 <code>sort_by_key</code> 中使用这个闭包：</p><p><span class="filename">文件名：src/main.rs</span></p><div class="language-rust,ignore,does_not_compile line-numbers-mode" data-highlighter="shiki" data-ext="rust,ignore,does_not_compile" data-title="rust,ignore,does_not_compile" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span>{{#rustdoc_include ../listings/ch13-functional-features/listing-13-08/src/main.rs}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><span class="caption">示例 13-8：尝试在 <code>sort_by_key</code> 上使用一个 <code>FnOnce</code> 闭包</span></p><p>这是一个刻意构造的、复杂且无效的方式，试图统计在对 <code>list</code> 进行排序时 <code>sort_by_key</code> 调用闭包的次数。该代码试图通过将闭包环境中的 <code>value</code>（一个 <code>String</code>）插入 <code>sort_operations</code> vector 来实现计数。闭包捕获了 <code>value</code>，然后通过将 <code>value</code> 的所有权转移给 <code>sort_operations</code> vector 的方式将其移出闭包。这个闭包只能被调用一次；尝试第二次调用它将无法工作，因为这时 <code>value</code> 已经不在闭包的环境中，无法被再次插入 <code>sort_operations</code> 中！因而，这个闭包只实现了 <code>FnOnce</code>。当我们尝试编译此代码时，会出现错误提示：<code>value</code> 不能从闭包中移出，因为闭包必须实现 <code>FnMut</code>：</p><div class="language-console line-numbers-mode" data-highlighter="shiki" data-ext="console" data-title="console" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{{#include ../listings/ch13-functional-features/listing-13-08/output.txt}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>报错指向了闭包体中将 <code>value</code> 移出环境的那一行。要修复此问题，我们需要修改闭包体，使其不会将值移出环境。在环境中维护一个计数器，并在闭包体中递增其值，是计算闭包被调用次数的一个更简单直接的方法。示例 13-9 中的闭包可以在 <code>sort_by_key</code> 中使用，因为它只捕获了 <code>num_sort_operations</code> 计数器的可变引用，因此可以被多次调用：</p><p><span class="filename">文件名：src/main.rs</span></p><div class="language-rust,noplayground line-numbers-mode" data-highlighter="shiki" data-ext="rust,noplayground" data-title="rust,noplayground" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span>{{#rustdoc_include ../listings/ch13-functional-features/listing-13-09/src/main.rs}}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><span class="caption">示例 13-9：允许在 <code>sort_by_key</code> 上使用一个 <code>FnMut</code> 闭包</span></p><p>当定义或使用涉及闭包的函数或类型时，<code>Fn</code> traits 十分重要。在下个小节中，我们将讨论迭代器。许多迭代器方法都接收闭包参数，因此在继续前，请记住这些闭包的细节！</p>`,83)]))}const l=s(a,[["render",c],["__file","ch13-01-closures.html.vue"]]),r=JSON.parse('{"path":"/ch13-01-closures.html","title":"ch13-01-closures","lang":"zh-CN","frontmatter":{"description":"闭包：可以捕获环境的匿名函数 ch13-01-closures.md commit a2cb72d3ad7584cc1ae3b85f715c877872f5e3ab Rust 的 闭包（closures）是可以保存在变量中或作为参数传递给其他函数的匿名函数。你可以在一个地方创建闭包，然后在不同的上下文中执行闭包运算。不同于函数，闭包允许捕获其被定义时所...","head":[["meta",{"property":"og:url","content":"https://LincDocs.github.io/trpl-zh-cn/ch13-01-closures.html"}],["meta",{"property":"og:site_name","content":"trpl-zh-cn"}],["meta",{"property":"og:description","content":"闭包：可以捕获环境的匿名函数 ch13-01-closures.md commit a2cb72d3ad7584cc1ae3b85f715c877872f5e3ab Rust 的 闭包（closures）是可以保存在变量中或作为参数传递给其他函数的匿名函数。你可以在一个地方创建闭包，然后在不同的上下文中执行闭包运算。不同于函数，闭包允许捕获其被定义时所..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"LincDocs\\",\\"url\\":\\"https://github.com/LincDocs/trpl-zh-cn/\\"}]}"]]},"git":{},"readingTime":{"minutes":15.29,"words":4587},"filePathRelative":"ch13-01-closures.md","excerpt":"<h2>闭包：可以捕获环境的匿名函数</h2>\\n<blockquote>\\n<p><a href=\\"https://github.com/rust-lang/book/blob/main/src/ch13-01-closures.md\\" target=\\"_blank\\" rel=\\"noopener noreferrer\\">ch13-01-closures.md</a><br>\\n<br><br>\\ncommit a2cb72d3ad7584cc1ae3b85f715c877872f5e3ab</p>\\n</blockquote>\\n<p>Rust 的 <strong>闭包</strong>（<em>closures</em>）是可以保存在变量中或作为参数传递给其他函数的匿名函数。你可以在一个地方创建闭包，然后在不同的上下文中执行闭包运算。不同于函数，闭包允许捕获其被定义时所在作用域中的值。我们将展示这些闭包特性如何支持代码复用和行为定制。</p>","autoDesc":true,"bioChainData":{"outlink":[],"backlink":[{"title":"README","link":"README.html"},{"title":"SUMMARY","link":"SUMMARY.html"}],"localMap":{"nodes":[{"id":"ch13-01-closures.md","value":{"title":"ch13-01-closures","path":"ch13-01-closures.md","outlink":[],"backlink":["README.md","SUMMARY.md"]}},{"id":"README.md","value":{"title":"README","path":"README.md","outlink":[],"backlink":[]}},{"id":"SUMMARY.md","value":{"title":"SUMMARY","path":"SUMMARY.md","outlink":[],"backlink":[]}}],"links":[{"source":"README.md","target":"ch13-01-closures.md"},{"source":"SUMMARY.md","target":"ch13-01-closures.md"}]}}}');export{l as comp,r as data};
